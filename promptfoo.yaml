prompts:
  - "Tell me more about startup tax exemption"

providers:
  - openai:gpt-3.5-turbo

tests:
  - vars:
      topic: entrepreneur
    assert:
      - type: contains
        value: "period of time"
      - type: javascript
        value: output.length < 2000
